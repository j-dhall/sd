#Override application properties for testing

#Create schema using JPA
#Drop schema after finishing running test cases
#possible values of spring.jpa.hibernate.ddl-auto: create-drop, validate
spring.jpa.hibernate.ddl-auto=validate